# =============================================================================
# CLIENT-SIDE ENVIRONMENT VARIABLES (NEXT_PUBLIC_*)
# These are exposed to the browser and should NEVER contain secrets
# =============================================================================

# Supabase Client Configuration (PUBLIC - safe for browser)
NEXT_PUBLIC_SUPABASE_URL=https://your-project.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=your-anon-key

# External API Base URLs (if needed client-side)
OFF_BASE=https://world.openfoodfacts.org

# =============================================================================
# SERVER-SIDE ENVIRONMENT VARIABLES
# These are ONLY available on the server and should contain sensitive data
# =============================================================================

# Supabase Admin Configuration (SERVER-ONLY - bypasses RLS)
# ⚠️ CRITICAL: This key has admin privileges - NEVER expose to client!
SUPABASE_SERVICE_ROLE_KEY=your-service-role-key

# Database Connection (SERVER-ONLY)
DATABASE_URL=postgresql://postgres:password@db.your-project.supabase.co:5432/postgres

# External Service URLs (SERVER-ONLY)
SOLVER_URL=https://your-solver.railway.app
SPOONACULAR_KEY=your-spoonacular-key

# AI Integration (SERVER-ONLY)
OPENAI_API_KEY=your-openai-key
OPENAI_MODEL=gpt-4

# =============================================================================
# SECURITY NOTES
# =============================================================================
# 
# 1. CLIENT vs SERVER variables:
#    - NEXT_PUBLIC_* variables are sent to the browser
#    - All other variables stay on the server
#
# 2. Supabase keys:
#    - ANON_KEY: Safe for browser, respects Row Level Security (RLS)
#    - SERVICE_ROLE_KEY: Admin access, bypasses RLS - SERVER ONLY!
#
# 3. Environment validation:
#    - Client vars are validated in lib/env.client.ts
#    - Server vars are validated in lib/env.server.ts
#
# 4. For production deployment:
#    - Use your hosting provider's environment variable interface
#    - Never commit actual keys to version control
#    - Rotate keys regularly for security
#